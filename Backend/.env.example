DB_HOST=localhost
DB_PORT=3306
DB_NAME=transporte_omar_godoy
DB_USER=root
DB_PASSWORD=changeme

PORT=3000
JWT_SECRET=please_change_me
JWT_EXPIRES=8h
# Solo para desarrollo: permite que Sequelize altere tablas existentes al iniciar
DB_ALTER_ON_BOOT=false

# Coma-separados; ejemplo para desarrollo (incluye localhost y 127.0.0.1)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
# Permitir IPs de red local (192.168.x.x, 10.x.x.x, 172.16-31.x.x) en puerto 5173 (útil para probar desde celular)
CORS_ALLOW_LAN_5173=true

# Opcional: credenciales de CEO inicial
CEO_EMAIL=ceo@example.com
CEO_PASSWORD=ceo123

# Email SMTP (opcional para enviar correos reales al CEO cuando se toma un viaje)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
# true para TLS implicito (puerto 465 normalmente)
SMTP_SECURE=false

# Tarea de vencidas
CHECK_VENCIDAS_ON_BOOT=true
CHECK_VENCIDAS_INTERVAL_MIN=1440

 # (Eliminado: soporte WhatsApp/Twilio y plantillas)

# IA (opcional)
ENABLE_CLAUDE_SONNET_4_5=false
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL_ID=claude-3-5-sonnet-latest